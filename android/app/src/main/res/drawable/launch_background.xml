<?xml version="1.0" encoding="utf-8"?>
<!-- WISE Clinical Camera startup splash: the brand aperture centred on Deep
     Navy, shown until the Flutter engine draws its first frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/wise_navy" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/wise_splash_logo" />
    </item>
</layer-list>
